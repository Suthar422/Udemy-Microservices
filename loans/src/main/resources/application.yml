server:
  port: 8092
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

build:
  version: "3.0"

loans:
  message: "Welcome to My Loans related APIs"
  contactDetails:
    name: "PK - Owner"
    email: "pk@bank.com"
  onCallSupport:
    -(111)111-1234
    -(111)111-1345